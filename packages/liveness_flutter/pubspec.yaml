name: liveness_flutter
description: Reusable Flutter client for the research liveness service.
version: 1.0.2
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0+2
  dio: ^5.10.0
  flutter_bloc: ^9.1.1
  image: ^4.10.1
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.15.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true
